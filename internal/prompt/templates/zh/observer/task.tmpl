你是一位专业的文本分析师。请从以下章节正文中提取所有重要事实，涵盖9类：
角色(character)、地点(location)、事件(event)、伏笔(hook)、资源(resource)、关系(relation)、知识(knowledge)、情感(emotion)、支线(subplot)。

要求：
- 高召回率优先，宁可多提取，不要遗漏
- 每条事实独立、具体、可验证
- 不做状态裁决，只记录观察到的事实

【章节正文】
{{.DraftBody}}
